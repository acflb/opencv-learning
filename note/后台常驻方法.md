## 后台常驻
#### 程序一直运行不关闭
import keyboard
import cv2  # 你的 OpenCV 逻辑

def do_click_task():
    print("触发啦喵！")
    # 你原来的点击逻辑放这里

keyboard.add_hotkey('ctrl+alt+p', do_click_task)
print("后台监听中，按 Ctrl+Alt+P 触发~")
keyboard.wait()  # 让程序一直挂着，不退出

#### 